# 1. Git 로컬 저장소 초기화 및 브랜치 생성
git init
git checkout -b interior-design

# 2. 대용량 바이너리 및 불필요한 가상환경 파일 제외를 위한 .gitignore 파일 작성
# (venv/ 및 ComfyUI/ 디렉토리 배제)

# 3. 프로젝트 핵심 소스코드 및 JSON 워크플로우 파일 스테이징(add)
git add .gitignore PRD.md download_models.py furniture_inpainting_workflow.json room_redesign_workflow.json test_nodes.py workflow_guide.md comfyui-helper-nodes

# 4. 로컬 초기 커밋 생성
git commit -m "feat: add ComfyUI interior-design workflows, custom helper nodes and PRD"

# 5. 원격 저장소 추가 및 원격 변경사항 강제 병합 (unrelated histories 허용)
git remote add origin https://github.com/takealook3/-.git
git pull origin interior-design --allow-unrelated-histories --no-edit

# 6. 원격 저장소의 interior-design 브랜치로 최종 푸시
git push -u origin interior-design
